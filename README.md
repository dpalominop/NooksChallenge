# nooks-ml-takehome

Check out `explore-data.ipynb` for an exploration of the data in `train_data.csv`.
Your goal is to return a new file `data/evaluation_preds.csv` that has the predictions for the corresponding calls in `data/evaluation_data.csv`.
